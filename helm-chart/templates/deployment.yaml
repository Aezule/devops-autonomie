apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "devops-autonomie.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "devops-autonomie.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "devops-autonomie.name" . }}
    spec:
      containers:
        - name: {{ include "devops-autonomie.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.port }}
