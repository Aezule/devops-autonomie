replicaCount: 1

image:
  repository: Aezule/devops-autonomie
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  annotations: {}
  hosts:
    - host: devops-autonomie.example.com
      paths:
        - /
  tls: []
